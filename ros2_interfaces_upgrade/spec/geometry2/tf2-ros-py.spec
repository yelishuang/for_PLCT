%bcond_without tests
%bcond_without weak_deps

%global debug_package %{nil}
%global __os_install_post %(echo '%{__os_install_post}' | sed -e 's!/usr/lib[^[:space:]]*/brp-python-bytecompile[[:space:]].*$!!g')
%global __provides_exclude_from ^/opt/ros/%{ros_distro}/.*$
%global __requires_exclude_from ^/opt/ros/%{ros_distro}/.*$

%define RosPkgName      tf2-ros-py

Name:           ros-%{ros_distro}-%{RosPkgName}
Version:        0.25.18
Release:        1%{?dist}%{?release_suffix}
Summary:        ROS tf2-ros-py package

Url:            http://www.ros.org/wiki/tf2_ros
License:        BSD
Source0:        %{name}_%{version}.orig.tar.gz

Requires: ros-%{ros_distro}-geometry-msgs
Requires: ros-%{ros_distro}-rclpy
Requires: ros-%{ros_distro}-sensor-msgs
Requires: ros-%{ros_distro}-std-msgs
Requires: ros-%{ros_distro}-tf2-msgs
Requires: ros-%{ros_distro}-tf2-py
Requires: ros-%{ros_distro}-ros-workspace

BuildRequires: ros-%{ros_distro}-ros-workspace

%if 0%{?with_tests}
BuildRequires: python3-pytest
BuildRequires: ros-%{ros_distro}-sensor-msgs
%endif

Provides:       %{name}-devel = %{version}-%{release}
Provides:       %{name}-doc = %{version}-%{release}
Provides:       %{name}-runtime = %{version}-%{release}

%description

    This package contains the ROS Python bindings for the tf2 library
  

%prep
%autosetup -p1

%build

# In case we're installing to a non-standard location, look for a setup.sh
# in the install tree and source it.  It will set things like
# CMAKE_PREFIX_PATH, PKG_CONFIG_PATH, and PYTHONPATH.
if [ -f "/opt/ros/%{ros_distro}/setup.sh" ]; then . "/opt/ros/%{ros_distro}/setup.sh"; fi
%py3_build

%install
# In case we're installing to a non-standard location, look for a setup.sh
# in the install tree and source it.  It will set things like
# CMAKE_PREFIX_PATH, PKG_CONFIG_PATH, and PYTHONPATH.
if [ -f "/opt/ros/%{ros_distro}/setup.sh" ]; then . "/opt/ros/%{ros_distro}/setup.sh"; fi
%py3_install -- --prefix "/opt/ros/%{ros_distro}"

%if 0%{?with_tests}
%check
# Look for a directory with a name indicating that it contains tests
TEST_TARGET=$(ls -d * | grep -m1 "\(test\|tests\)" ||:)
if [ -n "$TEST_TARGET" ] && %__python3 -m pytest --version; then
# In case we're installing to a non-standard location, look for a setup.sh
# in the install tree and source it.  It will set things like
# CMAKE_PREFIX_PATH, PKG_CONFIG_PATH, and PYTHONPATH.
if [ -f "/opt/ros/%{ros_distro}/setup.sh" ]; then . "/opt/ros/%{ros_distro}/setup.sh"; fi
%__python3 -m pytest $TEST_TARGET || echo "RPM TESTS FAILED"
else echo "RPM TESTS SKIPPED"; fi
%endif

%files
/opt/ros/%{ros_distro}

%changelog
* Sat Dec 27 2025 yelishuang <lishuang.ros@isrc.iscas.ac.cn> - 0.25.18-1
- Update to 0.25.18
- Add .hpp headers alongside .h headers for C++ compatibility
- Fix issue #328: tf2_ros/buffer.hpp not found
- Sync with ROS 2 Humble official release
* Thu May 04 2023 Chris Lalancette clalancette@openrobotics.org - 0.25.2-1
- Autogenerated by ros-porting-tools
